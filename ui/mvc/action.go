package mvc

// Action represents an arbitrary operation that can be performed by a
// Reducer. In most cases, though not necessarily, an Action would result
// in the modification of a model.
type Action interface{}
